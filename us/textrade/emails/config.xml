<?xml version="1.0" encoding="UTF-8"?>

<email-settings>

    <host>{host}</host>

    <port>{port}</port>

    <sender>{email}</sender>

    <sender-mask>Textrade</sender-mask>

    <password>{password}</password>

</email-settings>
